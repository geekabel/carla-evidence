"""Shared test fixtures: canonical BBAs, small frames, regression vectors.

Populated alongside Phase 1 so ``small_frames.py`` and ``canonical_bbas.py`` host
roughly 30 reference BBAs (vacuous, categorical, bayesian, conflictual pairs from
Shafer / Smets / Smarandache / Denoeux) that all unit, property, and regression
tests share.
"""
