"""CARLA adapter (Layer 4, optional).

Sensor wrappers, an :class:`EgoVehicle` abstraction, an HDF5 recorder, and four
canonical scenarios (intersection occlusion, dense traffic, V2X packet loss,
adversarial ghost vehicle). Importing this subpackage requires the ``[carla]`` extra
(``pip install "carla-evidence[carla]"``). Lands in v0.6.0 (Phase 6).
"""
