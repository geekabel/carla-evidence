"""Canonical CARLA scenarios shipped with the library (Phase 6).

Planned modules:

- ``intersection_occlusion`` — 4-way intersection, pedestrian occluded by parked vehicle.
- ``dense_traffic`` — urban dense traffic with ambiguous multi-target association.
- ``v2x_packet_loss`` — two ego vehicles with configurable packet-loss rate.
- ``adversarial_ghost`` — injection of a ghost vehicle through the V2X channel.
"""
