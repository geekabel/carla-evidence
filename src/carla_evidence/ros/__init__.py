"""ROS 2 adapter (Layer 4, optional).

Provides a custom ``MassFunctionMsg`` (sparse focal-element encoding) and streaming
nodes (``BBABuilderNode``, ``BBAFusionNode``). Requires ``rclpy`` from a ROS 2 distro;
not available via PyPI. Lands in v0.7.0 (Phase 7).
"""
