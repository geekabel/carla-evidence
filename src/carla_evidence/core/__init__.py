"""Core layer (Layer 1): frame of discernment, mass functions, powerset utilities.

This package depends only on ``numpy`` and ``scipy``. Modifications here have
project-wide impact — changes require explicit user confirmation per ``CLAUDE.md``.

Symbols are intentionally not re-exported yet; the public API is fixed in v0.1.0
(Phase 1).
"""
