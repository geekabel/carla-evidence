"""Visualization layer (Layer 4, optional): matplotlib + Plotly plots.

Includes Venn-style focal-element bar charts (small Theta), conflict trajectories
``K(t)``, evidential occupancy-grid heatmaps via BetP, and uncertainty
decompositions (non-specificity vs discord). Lands in v0.8.0 (Phase 8).
"""
