"""Reproducible benchmark suite (Layer 5).

Five canonical CARLA scenarios with ground truth versioned via Zenodo, evaluated under
GOSPA / OSPA-squared / ghost-track count / conflict-mass-time / ignorance evolution.
Invocable as ``python -m carla_evidence.benchmarks run --rule pcr6 --scenario
dense_traffic --output report.html``. Lands in v0.9.0 (Phase 9).
"""
